FAILED TESTS

1. >>> INPUT:
(anything) > 2
(anything) >2
<<<
>>> FIXED?
NO
<<<

>>> RESULT: 
crashes, getting stuck parsing fails, duplicating minishell process, jsut random shitm
<<<

>>> OUTPUT:
various kinds of nonsense, memory errors gallore
<<<

>>> EDGE CASES 

- 'export empty= ""' -> 'echo hi > $EMPTY'
- '<<<eof' -> cntrl+D -> 'echo $?'
- '>ls' -> 'ls' -> './les'
- '>haha | ls'
- 'export empty=" "' -> 'echo hi > $EMPTY' -> '$EMPTY echo hi'
- export $BLA>hey   (we need to check if exporting with a pipe or something in the name breaks)
- >hey ls               (need to check how we can make a default if the input is missing for a redirect) 
- echo "$USER$USER"     (needs to expand to 'usernameusername', DONE)
